{
  "name": "web",
  "$schema": "../../node_modules/nx/schemas/project-schema.json",
  "projectType": "application",
  "sourceRoot": "apps/web/src",
  "targets": {
    "build.client": {
      "executor": "@nx/vite:build",
      "outputs": ["{workspaceRoot}/dist/apps/web"],
      "options": {
        "outputPath": "./dist/apps/web/client",
        "configFile": "./apps/web/vite.config.mts"
      }
    },
    "build.preview": {
      "executor": "nx:run-commands",
      "dependsOn": ["build.client"],
      "outputs": ["{workspaceRoot}/dist/apps/web/server"],
      "options": {
        "command": "vite build --ssr apps/web/src/entry.preview.tsx -c apps/web/vite.config.mts",
        "cwd": "{workspaceRoot}"
      }
    },
    "build": {
      "executor": "nx:noop",
      "dependsOn": ["build.client", "build.server"],
      "outputs": ["{workspaceRoot}/dist/apps/web"]
    },
    "serve": {
      "executor": "@nx/vite:dev-server",
      "options": {
        "buildTarget": "web:build.client",
        "mode": "ssr",
        "port": 5173
      }
    },
    "preview": {
      "executor": "@nx/vite:preview-server",
      "dependsOn": ["build.preview"],
      "options": {
        "buildTarget": "web:build.client",
        "port": 4173
      }
    }
  }
}
